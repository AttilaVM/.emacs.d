# -*- mode: snippet -*-
# name: code
# key: c
# expand-env: ((yas-indent-line nil))
# --
#+begin_src ${1:$$(yas-choose-value '(
						"emacs-lisp"
						"shell"
						"shell :dir /ssh:root@localhost:/tmp"
						"python"
						"mongo :host localhost :port 27017 :db"
						"nix"))} $2 $0
#+end_src